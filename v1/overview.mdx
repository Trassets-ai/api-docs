---
title: "Trassets Data API v1 (Preview)"
sidebarTitle: "Overview"
description: "Preview of the upcoming versioned Trassets Data API. Content is a placeholder until the /v1/ rollout is confirmed."
---

<Warning>
  This is a placeholder. `/v1/` is not live yet — the API team has not confirmed a rollout date. See [Versioning](/concepts/versioning) for the current policy and deprecation plan for unversioned paths.
</Warning>

Once `/v1/` ships, this section will mirror the current documentation with `/v1/`-prefixed paths and any breaking changes introduced at that point.
