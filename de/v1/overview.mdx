---
title: "Trassets Data API v1 (Preview)"
sidebarTitle: "Übersicht"
description: "Vorschau auf die kommende versionierte Trassets Data API. Inhalt ist ein Platzhalter, bis der /v1/-Rollout bestätigt ist."
---

<Warning>
  Dies ist ein Platzhalter. `/v1/` ist noch nicht live — das API-Team hat noch kein Rollout-Datum bestätigt. Siehe [Versionierung](/de/concepts/versioning) für die aktuelle Richtlinie und den Deprecation-Plan für unversionierte Pfade.
</Warning>

Sobald `/v1/` live ist, spiegelt dieser Bereich die aktuelle Dokumentation mit `/v1/`-Pfaden und den zu diesem Zeitpunkt eingeführten Breaking Changes.
